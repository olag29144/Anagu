{
  "name": "anagu",
  "version": "1.0.0",
  "description": "Anagu Land Administration Framework — permissioned full-stack land title system",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend/web",
    "build": "npm run build --prefix frontend/web",
    "start": "npm run start --prefix frontend/web",
    "lint": "npm run lint --prefix frontend/web",
    "postinstall": "npm install --prefix frontend/web"
  },
  "workspaces": [
    "frontend/web"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
